<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Fractal_Nirvana"
             x:Class="Fractal_Nirvana.MainPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <local:PreviewImage
                x:Name="preview"
                Grid.ColumnSpan="2"/>
            <Label BindingContext="{x:Reference Name=preview}"
                Text="{Binding Width}" 
                Grid.Row="1" 
                Grid.Column="0"/>
            <Label BindingContext="{x:Reference Name=preview}"
                Text="{Binding Height}" 
                Grid.Row="1" 
                Grid.Column="1"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>